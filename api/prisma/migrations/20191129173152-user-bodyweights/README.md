# Migration `20191129173152-user-bodyweights`

This migration has been generated by Andy Kay at 11/29/2019, 5:31:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191122105923-make-username-unique..20191129173152-user-bodyweights
--- datamodel.dml
+++ datamodel.dml
@@ -1,22 +1,23 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:root@localhost:3306/fitfam"
 }
 generator photon {
   provider = "photonjs"
 }
 model User {
-  id        String    @default(cuid()) @id @unique
-  email     String    @unique
-  name      String
-  username  String    @unique
-  picture   String
-  workouts  Workout[]
-  createdAt DateTime  @default(now())
-  updatedAt DateTime  @updatedAt
+  id          String       @default(cuid()) @id @unique
+  email       String       @unique
+  name        String
+  username    String       @unique
+  picture     String
+  workouts    Workout[]
+  bodyweights Bodyweight[]
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
 }
 model Workout {
   id        String       @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20191129173152-user-bodyweights)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191129173152-user-bodyweights'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
