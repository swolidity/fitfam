# Migration `20191205173408-add-workout-slug`

This migration has been generated by Andy Kay at 12/5/2019, 5:34:08 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `fitfam`.`Workout` ADD COLUMN `slug` varchar(191) NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191205173157-make-exercise-slug-unique..20191205173408-add-workout-slug
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photon {
   provider      = "photonjs"
@@ -24,8 +24,9 @@
 model Workout {
   id        String       @default(cuid()) @id @unique
   title     String
+  slug      String
   user      User
   logs      WorkoutLog[]
   createdAt DateTime     @default(now())
   updatedAt DateTime     @updatedAt
```

## Photon Usage

You can use a specific Photon built for this migration (20191205173408-add-workout-slug)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191205173408-add-workout-slug'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
