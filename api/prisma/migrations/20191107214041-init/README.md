# Migration `20191107214041-init`

This migration has been generated by Andy Kay at 11/7/2019, 9:40:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `fitfam`.`User` (
  `email` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `fitfam`.`Workout` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `title` varchar(191) NOT NULL DEFAULT '' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `fitfam`.`WorkoutLog` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `reps` int NOT NULL DEFAULT 0 ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `weight` Decimal(65,30) NOT NULL DEFAULT 0 ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `fitfam`.`Exercise` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `fitfam`.`Workout` ADD COLUMN `user` varchar(191)  ,
ADD FOREIGN KEY (`user`) REFERENCES `fitfam`.`User`(`id`) ON DELETE SET NULL;

ALTER TABLE `fitfam`.`WorkoutLog` ADD COLUMN `exercise` varchar(191)  ,
ADD FOREIGN KEY (`exercise`) REFERENCES `fitfam`.`Exercise`(`id`) ON DELETE SET NULL,
ADD COLUMN `workout` varchar(191)  ,
ADD FOREIGN KEY (`workout`) REFERENCES `fitfam`.`Workout`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.id` ON `fitfam`.`User`(`id`)

CREATE UNIQUE INDEX `User.email` ON `fitfam`.`User`(`email`)

CREATE UNIQUE INDEX `Workout.id` ON `fitfam`.`Workout`(`id`)

CREATE UNIQUE INDEX `WorkoutLog.id` ON `fitfam`.`WorkoutLog`(`id`)

CREATE UNIQUE INDEX `Exercise.id` ON `fitfam`.`Exercise`(`id`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191107214041-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+datasource db {
+  provider = "mysql"
+  url      = "mysql://root:root@localhost:3306/fitfam"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id       String    @default(cuid()) @id @unique
+  email    String    @unique
+  name     String
+  workouts Workout[]
+}
+
+model Workout {
+  id        String       @default(cuid()) @id @unique
+  title     String
+  user      User
+  logs      WorkoutLog[]
+  createdAt DateTime     @default(now())
+  updatedAt DateTime     @updatedAt
+}
+
+model WorkoutLog {
+  id        String   @default(cuid()) @id @unique
+  exercise  Exercise
+  reps      Int
+  weight    Float
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Exercise {
+  id        String   @default(cuid()) @id @unique
+  name      String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191107214041-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191107214041-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
