# Migration `20191118123024-fb-picture`

This migration has been generated by Andy Kay at 11/18/2019, 12:30:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `fitfam`.`User` ADD COLUMN `picture` varchar(191) NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191107214207-add-timestamps-to-user..20191118123024-fb-picture
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:root@localhost:3306/fitfam"
 }
 generator photon {
   provider = "photonjs"
@@ -10,8 +10,9 @@
 model User {
   id        String    @default(cuid()) @id @unique
   email     String    @unique
   name      String
+  picture   String
   workouts  Workout[]
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191118123024-fb-picture)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191118123024-fb-picture'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
