# Migration `20191107214207-add-timestamps-to-user`

This migration has been generated by Andy Kay at 11/7/2019, 9:42:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `fitfam`.`User` ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
ADD COLUMN `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191107214041-init..20191107214207-add-timestamps-to-user
--- datamodel.dml
+++ datamodel.dml
@@ -1,18 +1,20 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:root@localhost:3306/fitfam"
 }
 generator photon {
   provider = "photonjs"
 }
 model User {
-  id       String    @default(cuid()) @id @unique
-  email    String    @unique
-  name     String
-  workouts Workout[]
+  id        String    @default(cuid()) @id @unique
+  email     String    @unique
+  name      String
+  workouts  Workout[]
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
 }
 model Workout {
   id        String       @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20191107214207-add-timestamps-to-user)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191107214207-add-timestamps-to-user'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
