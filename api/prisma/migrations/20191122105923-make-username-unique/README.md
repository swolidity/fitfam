# Migration `20191122105923-make-username-unique`

This migration has been generated by Andy Kay at 11/22/2019, 10:59:23 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `fitfam`.`Bodyweight` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `user` varchar(191)  ,
 FOREIGN KEY (`user`) REFERENCES `fitfam`.`User`(`id`) ON DELETE SET NULL,
  `weight` Decimal(65,30) NOT NULL DEFAULT 0 ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE UNIQUE INDEX `User.username` ON `fitfam`.`User`(`username`)

CREATE UNIQUE INDEX `Bodyweight.id` ON `fitfam`.`Bodyweight`(`id`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191120152202-username..20191122105923-make-username-unique
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:root@localhost:3306/fitfam"
 }
 generator photon {
   provider = "photonjs"
@@ -10,9 +10,9 @@
 model User {
   id        String    @default(cuid()) @id @unique
   email     String    @unique
   name      String
-  username  String
+  username  String    @unique
   picture   String
   workouts  Workout[]
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
@@ -40,5 +40,13 @@
   id        String   @default(cuid()) @id @unique
   name      String
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
+}
+
+model Bodyweight {
+  id        String   @default(cuid()) @id @unique
+  weight    Float
+  user      User
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191122105923-make-username-unique)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191122105923-make-username-unique'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
