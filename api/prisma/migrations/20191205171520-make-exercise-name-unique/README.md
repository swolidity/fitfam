# Migration `20191205171520-make-exercise-name-unique`

This migration has been generated by Andy Kay at 12/5/2019, 5:15:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX `Exercise.name` ON `fitfam`.`Exercise`(`name`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191205135808-create-profile-songs-table..20191205171520-make-exercise-name-unique
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photon {
   provider      = "photonjs"
@@ -41,9 +41,9 @@
 }
 model Exercise {
   id        String   @default(cuid()) @id @unique
-  name      String
+  name      String   @unique
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191205171520-make-exercise-name-unique)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191205171520-make-exercise-name-unique'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
