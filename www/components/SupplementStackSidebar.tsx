import React from "react";
import { Box, Heading } from "@chakra-ui/core";
import SupplementsPage from "../pages/supplements";

const SupplementStackSidebar = () => (
  <Box>
    <Heading as="h2" size="xs">
      SUPPLEMENT STACK
    </Heading>
  </Box>
);

export default SupplementStackSidebar;
